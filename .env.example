# Redis connection (used by examples, benchmark, and tests)
REDIS_URL=redis://127.0.0.1:6379

# Default algorithm when a route does not override it.
# One of: sliding-window-counter | token-bucket | sliding-window-log | fixed-window
RATE_LIMIT_ALGORITHM=sliding-window-counter

# HTTP port for the example servers
PORT=3000
